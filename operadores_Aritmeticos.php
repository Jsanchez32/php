<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <?php
        $x = 20;
        $y = 3;
        $z = null;

/*         $z = $x + $y;
        $z = $x - $y;
        $z = $x * $y;
        $z = $x / $y;
        $z = $x ** $y;
        $z = $x + $y;
        $z = $x + $y;
        $z = $x + $y;
 */
        // 2. increment/decrement operators//

        $counter =10;

/*         $counter= $counter + 1;
        $counter++;
        $counter--; */

        // En dos valores o mas//

        $counter +=4;

        echo "{$counter}"

    ?>
</body>
</html>